#include <stdio.h>
#include <stdlib.h>
typedef struct Node
{
    int data;
    struct Node *next;
}Node;
int main()
{
    struct Node *head=(struct Node *)malloc(sizeof(struct Node));

    
    return 0;
}