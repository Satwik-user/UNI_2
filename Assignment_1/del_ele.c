#include <stdio.h>
int main()
{
    int n;
    printf("Enter size of array:");
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++)
    {
        printf("Enter element:");
        scanf("%d",&arr[i]);
    }
    int del_pos;
    pritnf("Enter position of element to be deleted:");
    scanf("%d",&del_pos);
    for(int i=del;i<n;i++)
    {

    }
    
    return 0;
}