#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n, key;
    printf("Enter array:");
    scanf("%d", &n);

    printf("Enter search key:");
    scanf("%d", &key);

    
    for(int i=0; i<n; i++)
    
    return 0;
}